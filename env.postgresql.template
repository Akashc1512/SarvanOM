# PostgreSQL Database Configuration Template
# Copy this to your .env file and fill in your actual values

# PostgreSQL Connection String (recommended)
DATABASE_URL=postgresql://[username]:[password]@[host]:[port]/[database]

# Individual PostgreSQL Environment Variables
PGHOST=[host]
PGPORT=[port]
PGUSER=[username]
PGPASSWORD=[password]
PGDATABASE=[database]

# Example (replace with your actual values):
# DATABASE_URL=postgresql://myuser:mypassword@localhost:5433/mydatabase
# PGHOST=localhost
# PGPORT=5433
# PGUSER=myuser
# PGPASSWORD=mypassword
# PGDATABASE=mydatabase

# Note: These values should match your docker-compose.yml configuration
# and the actual PostgreSQL container settings
